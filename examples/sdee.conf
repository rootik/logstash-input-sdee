input {
  sdee {
    method => get
    url => "https://ciscoips/cgi-bin/sdee-server"
    auth => {
          user => "cisco"
          password => "p@ssw0rd"
        }
      }
    request_timeout => 60
    interval => 60
    codec => "plain" # This is CSV data, so just let it be plain for now
    metadata_target => "_sdee_metadata"
  }
}

filter {
  xml {
  }
}

output {
  stdout {
    codec => rubydebug
  }
}